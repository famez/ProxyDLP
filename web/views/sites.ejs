<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Monitored Sites Dashboard</title>
  <link rel="stylesheet" href="/css/tailwind.css" />

</head>
<body class="bg-gradient-to-br from-indigo-100 via-purple-100 to-pink-100 min-h-screen flex items-center justify-center p-6">
  <main class="w-full max-w-6xl bg-white p-8 rounded-2xl shadow-xl">
    <h1 class="text-4xl font-bold text-indigo-700 mb-10 text-center">Monitored Sites</h1>

    <!-- Reject Traffic by Default Checkbox -->
    <div class="flex items-center gap-2 mb-4">
      <input
      type="checkbox"
      id="rejectTraffic"
      class="h-4 w-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500"
      <%= rejectTraffic ? 'checked' : '' %>> <!-- Backend-rendered state -->

      
      <label for="rejectTraffic" class="text-sm font-medium text-indigo-700">
        Reject traffic by default
      </label>

      <!-- Tooltip -->
      <div class="relative group">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-4 w-4 text-indigo-500 cursor-pointer"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M12 18h.01M12 6v6m0 6a9 9 0 110-18 9 9 0 010 18z" />
        </svg>
        <div
          class="absolute left-6 top-1/2 -translate-y-1/2 w-72 bg-white text-gray-700 text-xs rounded-lg shadow-lg p-3 opacity-0 group-hover:opacity-100 transition"
        >
          If checked, any traffic intercepted by the proxy that is not intended for a monitored site
          will be automatically <span class="font-semibold text-red-600">rejected</span>.
          If unchecked, it will be <span class="font-semibold text-green-600">allowed</span>.
        </div>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead>
          <tr class="bg-indigo-50">
            <th class="px-6 py-3 text-left text-sm font-semibold text-indigo-700 uppercase tracking-wider">Site</th>
            <th class="px-6 py-3 text-left text-sm font-semibold text-indigo-700 uppercase tracking-wider">Watched URLs</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-100">

          <% site_docs.forEach(site => { %>
            <tr class="hover:bg-indigo-50 transition">
              <td class="px-6 py-4 flex items-center gap-3 text-indigo-700 font-medium">
                <!-- Globe icon -->
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m0 14v1m8-8h1M4 12H3m15.364-6.364l.707.707M6.343 17.657l-.707.707m0-12.728l.707-.707M17.657 17.657l.707.707M12 8a4 4 0 100 8 4 4 0 000-8z" />
                </svg>
                <%= site.name %>
              </td>
              <td class="px-6 py-4 text-sm text-gray-700 space-y-1">
                <% site.urls.forEach(url => { %>
                  <div><%= url %></div>
                <% }) %>
              </td>
            </tr>
          <% }) %>

        </tbody>
      </table>
    </div>
    
    <div class="mt-10 p-6 bg-indigo-50 rounded-xl shadow-inner">
      <h2 class="text-xl font-semibold text-indigo-700 mb-4">Generate PAC File</h2>
      <form action="/generate-pac" method="POST" class="flex flex-col sm:flex-row gap-4 items-start sm:items-center">
        <input
          name="proxy"
          type="text"
          placeholder="Enter proxy address and port (e.g., proxy.contoso.com:8080)"
          required
          class="w-full sm:w-1/2 px-4 py-2 border border-indigo-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
        />
        <button
          type="submit"
          class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold px-6 py-2 rounded-lg transition"
        >
          Generate PAC File
        </button>
      </form>
    </div>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const rejectCheckbox = document.getElementById('rejectTraffic');

        // Handle Reject Traffic Change
        rejectCheckbox.addEventListener('change', async (event) => {
          const rejectTraffic = event.target.checked;

          try {
            const response = await fetch('/sites/reject-traffic', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
                'X-Requested-With': 'XMLHttpRequest'
              },
              body: JSON.stringify({ reject_traffic: rejectTraffic })
            });

            if (!response.ok) {
              throw new Error('Failed to update reject traffic setting');
            }

            console.log('Reject traffic setting updated:');
          } catch (error) {
            console.error('Error updating reject traffic setting:', error);
            alert('Failed to update setting. Please try again.');
            event.target.checked = !rejectTraffic; // Rollback checkbox state
          }
        });
      });
    </script>

  </main>
</body>
</html>
