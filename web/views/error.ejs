<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || 'Error' %></title>
  <link rel="stylesheet" href="/css/tailwind.css" />
</head>
<body class="bg-red-50 flex items-center justify-center min-h-screen">
  <div class="bg-white p-8 rounded-xl shadow-md text-center max-w-md">
    <h1 class="text-3xl font-bold text-red-600 mb-4"><%= title || 'Error' %></h1>
    <p class="text-gray-700 mb-6"><%= message || 'An error occurred.' %></p>
    <a href="/" class="inline-block bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700 transition">
      Return to Home
    </a>
  </div>
</body>
</html>
