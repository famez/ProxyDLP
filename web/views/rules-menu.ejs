<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rule Manager Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes pulseList {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.02); }
    }
    li[data-pulse="true"] {
      animation: pulseList 2s ease-in-out infinite;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-indigo-100 via-purple-100 to-pink-100 min-h-screen flex items-center justify-center p-4">
  <main class="w-full max-w-6xl p-8 bg-white rounded-2xl shadow-xl">
    <h1 class="text-4xl font-bold text-indigo-700 mb-10 text-center">Rule Manager</h1>

    <!-- Cards Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      
      <!-- Regex Rules Card -->
      <a href="/rules/regex" class="group p-6 bg-gray-50 rounded-xl shadow border border-indigo-100 hover:shadow-lg transition hover:bg-indigo-50 flex items-center gap-4">
        <div class="p-3 bg-indigo-100 text-indigo-700 rounded-full">
          <!-- Regex Icon (Bracket-like) -->
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 18l6-6-6-6M8 6l-6 6 6 6" />
          </svg>
        </div>
        <div>
          <p class="text-lg font-semibold text-indigo-700 group-hover:underline">Regex Rules</p>
          <p class="text-sm text-gray-500">Manage regular expression-based rules</p>
        </div>
      </a>

      <!-- Topic Rules Card -->
      <a href="/rules/topic" class="group p-6 bg-gray-50 rounded-xl shadow border border-green-100 hover:shadow-lg transition hover:bg-green-50 flex items-center gap-4">
        <div class="p-3 bg-green-100 text-green-700 rounded-full">
          <!-- Topic Icon (Tag) -->
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M7 7h.01M3 7a4 4 0 014-4h10a4 4 0 014 4v10a4 4 0 01-4 4H7a4 4 0 01-4-4V7z" />
          </svg>
        </div>
        <div>
          <p class="text-lg font-semibold text-green-700 group-hover:underline">Topic Rules</p>
          <p class="text-sm text-gray-500">Define topic match patterns</p>
        </div>
      </a>

      <!-- Yara Rules Card -->
      <a href="/rules/yara" class="group p-6 bg-gray-50 rounded-xl shadow border border-yellow-100 hover:shadow-lg transition hover:bg-yellow-50 flex items-center gap-4">
        <div class="p-3 bg-yellow-100 text-yellow-700 rounded-full">
          <!-- Yara Icon (Shield) -->
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M12 2l7 4v6c0 5.25-3.5 9.25-7 10-3.5-.75-7-4.75-7-10V6l7-4z" />
          </svg>
        </div>
        <div>
          <p class="text-lg font-semibold text-yellow-700 group-hover:underline">Yara Rules</p>
          <p class="text-sm text-gray-500">Manage advanced detection with YARA rules</p>
        </div>
      </a>
      
    </div>
  </main>
</body>
</html>
